# [面试经验] 数据🧱24年10月算法新题

`Post ID: 1090864` | `Author: 匿名用户-RWBIF` | `Published: 2024-10-9 23:54:01`

Source: https://www.1point3acres.com/bbs/thread-1090864-1-1.html

---

## Post

没在之前的面筋看到，这是完整的题目，难度还是比较大的，需要根据这个编码逻辑，写一个encode， 再写一个decode。 需要编译通过。encode相对比较难，有好几个判断条件。 会提供BP和RLE的定义和需要implement的API
# Run-Length Encoding (RLE) : Encoding scheme where integers that have the same value can be represented as a single value with a count, e.g. 1, 1, 1, 1, 1 can be written as 1 (5).
# Bit-Packing (BP) : Encoding scheme where smaller integer values can be represented without using the full 4 bytes of data. For example, all numbers in 2, 3, 4, 5 fit within 3 bits, so we can encode ("pack") them into 12 bits or at most 2 bytes thus reducing data 8x.
# Run is a sequence of values that are encoded using RLE or BP. All values in a run have the same encoding but runs themselves can interleave, e.g. [RLE, BP, RLE, RLE, BP, ...].
#  1. All values have to be placed into runs, and value order cannot change.
#  2. A Run-Length encoded run has at least 8 values (only the last run can have
#     fewer values).
#  3. Once started, a Run-Length encoded run should extend as far as possible
#     (it should continue as long as the input values are repeating).
#  4. A Bit-Packing encoded run has exactly 8 values, i.e. once it is started,
#     it is required to fill all 8 values (only the last run can have fewer values).
#  5. Run-Length encoding is our default scheme.  We only use Bit-Packing encoding
#     when we cannot apply Run-Length (because there is not enough repeated values).
# [1, 1, 1] will be encoded as RLE[1, 3].
# [1, 1, 1, 1, 2, 3, 4, 5] will be encoded as BP[1, 1, 1, 1, 2, 3, 4, 5].
# [1, 1, 1, 1, 1, 1, 1, 1, 2, 3, 4, 5] will be encoded as RLE[1, 8], BP[2, 3, 4, 5].
# [1, 1, 1, 1, 1, 1, 1, 1, 1, 2, 3, 4, 5] will be encoded as RLE[1, 9], BP[2, 3, 4, 5].
# This file contains all of the necessary code for the exercise.
#
# Your goal is to implement Encoder and Decoder specification by combining
# RLE and BP encodings.
#
# You should implement all methods containing `# TODO` comment.
# You may use the API helper methods and classes provided below.
复制代码
求点米看面经🧎

---

## Replies

**#2 匿名用户-VWZQQ** | `2025-7-27 12:35:39`

暴躁的蛋挞 发表于 2025-5-9 05:54
刚面完这个题目，比较恶心。。补充一些细节。
C++开局200多行：大几十行描述+100多行类的定义/输入输出 ...
想问下, 有一个情况说得不是很清楚, 如果最开始是8个1, 然后是3个2, [1,1,1,1,1,1,1,1,2,2,2], 我的理解输出应该是RLE[1,8], RLE[2,3]吧? 而不是RLE[1,8], BP[2,2,2]. 我的理解是这时RLE和BP都可以用, 因为都是末尾且不足8位, 但应该默认先使用RLE?

**#3 匿名用户-VWZQQ** | `2025-7-27 12:33:00`

VKD_job 发表于 2025-4-13 23:50
楼主您好，请问 [2,3,1,1,1,1,1,1,1,1,4,5]这种情况，输出应该是什么？按照 RLE优先编1吗？那好像就不满足 ...
我的理解是必须先用BP, 因为最开始2,3就不一样, BP存满8个数, 再接下去判断, 所以你的这个例子应该是2个BP

**#4 2025-5-9 06:54:46**

刚面完这个题目，比较恶心。。补充一些细节。
C++开局200多行：大几十行描述+100多行类的定义/输入输出，一共有5个类（有的构造函数还不全，得自己补）。
Encoder类似流式输入，每次append 1个数。开始就有followup：已知流式输入很多且有相同的怎么办？（存数和连续出现的次数）
然后写Encoder：2个方法的处理，类似大模拟，没什么算法但细节多。2个方式都有限制且会互相影响。
BP定义模糊且意义不明。沟通了一阵自己选了简单的定义（直接存8个数）。
20分钟以上在理解题意和类的架构和澄清，写代码时也得来回看要求和类定义。
磕磕绊绊写完了Encoder但测试不太对，没时间debug。
没提前看过的话，补全所有函数非常困难。

**#5 匿名用户-A6SYA** | `2024-10-24 00:50:41`

请问楼主方便share target level嘛

**#6 2024-12-27 00:47:30**

楼主你好，BP是只能8个value的话，如果有的value大于4个bit，那么一个32-bit的integer就放不下了，这种情况应该如果处理呢？

**#7 2025-3-20 14:46:02**

独孤辰涛 发表于 2024-12-27 00:47
楼主你好，BP是只能8个value的话，如果有的value大于4个bit，那么一个32-bit的integer就放不下了，这种情况 ...
对 他这个题目有点不太严谨，我觉得遇到的话，可以嘴遁一下，如果一个要编码的数字大于15，那么我们应该给予他特殊处理，比如自己占有一个BP，或者动态的设计BP的长度。
但他的constrain当中又说了，BP一旦启动就放8个数字。
所以我觉得可以适当假设他的数组中的数字都是合规的可以fit in 4个bits，也就是小于15的。

**#8 2025-4-14 00:50:51**

楼主您好，请问 [2,3,1,1,1,1,1,1,1,1,4,5]这种情况，输出应该是什么？按照 RLE优先编1吗？那好像就不满足所有的value都要被encode；还是在2，3的时候启动BP？那好像又不满足优先RLE...

**#9 匿名用户-DGIYL** | `2025-5-22 10:54:16`

我也面了这题，之前没有提前看，我大概花了半小时理解题意，感觉实在是又臭又长，最后也没写完。

**#10 2025-7-27 15:32:21**

匿名用户 发表于 2025-07-27 12:35:39
想问下, 有一个情况说得不是很清楚, 如果最开始是8个1, 然后是3个2, , 我的理解输出应该是RLE, RLE吧? 而不是RLE, BP. 我的理解是这时R
应该是没有具体说明。
怀疑题目描述是故意模糊的。一是看沟通能力，二是如果有背题痕迹面试官会直接让你用另一个方式实现。
